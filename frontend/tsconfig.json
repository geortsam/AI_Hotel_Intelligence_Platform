{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noEmit": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] },
    "types": []
  },
  "include": ["src"],
  "//": "A `.node.test.ts` runs under vitest like any other test, but reads the source tree from disk and so needs Node's globals. `types: []` above is deliberate -- it keeps `process` and `node:fs` out of code that ships to a browser -- so those files are typechecked by tsconfig.node.json instead of relaxing the rule for everything.",
  "exclude": ["src/**/*.node.test.ts"]
}
