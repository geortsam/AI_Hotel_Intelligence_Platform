# Offline ML dependencies. Installed by CI's quality-gates job, so that Python 3.14
# compatibility is verified rather than assumed.
#
# Stage 6.7 note: this is no longer the only place scikit-learn is declared. The production
# image needs it too -- to deserialise the approved artifact and call `predict` -- so
# `backend/requirements.txt` now carries the SAME pin, and a test asserts the two agree. This
# file remains the offline declaration: it is what the evaluation, validation and artifact
# pipelines install, and it is not what the image installs.
#
#   pip install -r ml/requirements-ml.txt
#
# Stage 6.3 pins scikit-learn and removes the pandas and NumPy declarations that had been here
# since Stage 1 and were installed by nothing. Nothing imports either directly; NumPy and SciPy
# arrive as scikit-learn's own requirements, which is the repository's usual policy -- direct
# dependencies pinned, their dependencies resolved at install time.
#
# scikit-learn is here for exactly one estimator, HistGradientBoostingRegressor: the smallest
# conventional learned regressor for a small tabular panel. No gradient-boosting library, no
# deep-learning framework and no LLM client is declared, and a test asserts none is imported.

scikit-learn==1.9.1
