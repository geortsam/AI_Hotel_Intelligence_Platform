"""AI Hotel Intelligence Platform backend.

``__version__`` lives here rather than in ``Settings``: it identifies the *code*, not the
*deployment*. Environment configuration answers "where am I running?", while the version
answers "what is running?" -- and the answer must not be something an operator can change
by editing a variable.
"""

__version__ = "0.1.0"
